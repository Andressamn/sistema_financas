# URL da API backend
# Em dev (rodando local): deixa em branco ou aponta pra localhost:3000
# Em produção (Vercel): definir como a URL do Render, ex: https://finance-api.onrender.com
VITE_API_URL=http://localhost:3000
